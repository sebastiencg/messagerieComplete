<form method="post" action="{{ path('app_relation_delete', {'id': relation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ relation.id) }}">
    <button class="btn">Delete</button>
</form>
